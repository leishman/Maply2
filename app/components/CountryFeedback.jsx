var React = require('react');
var Navigation = require('react-router').Navigation;
var State = require('react-router').State;

var CountryFeedback = React.createClass({

  render: function() {
    return (
      <div>
        Country Feedback form
      </div>
    )
  }
});